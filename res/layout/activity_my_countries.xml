<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context=".MyCountries" >

    <Button
        android:id="@+id/buttonAddCountry"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentRight="true"
        android:layout_alignParentTop="true"
        android:background="@color/myColorBlue"
        android:onClick="onClick"
        android:padding="10dp"
        android:text="@string/buttonAddCountry" />

    <ListView
        android:id="@+id/listViewCountries"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_alignLeft="@+id/textViewHeader"
        android:layout_below="@+id/buttonAddCountry"
        android:layout_marginTop="18dp" />

    <TextView
        android:id="@+id/textViewHeader"
        style="?android:attr/listSeparatorTextViewStyle"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignBottom="@+id/buttonAddCountry"
        android:layout_alignParentLeft="true"
        android:text="@string/textViewHeader"
        android:textSize="20sp"
        android:textStyle="bold" />

</RelativeLayout>
